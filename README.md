# BCIS-Algorithm-Official
Traditional insertion sort algorithms are strictly unidirectional, processing elements sequentially from i=0 to n-1. This paper introduces a symmetric processing model that handles both the lower and upper bounds of the unsorted sub array simultaneously. 
